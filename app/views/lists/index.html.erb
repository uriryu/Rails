<h1>投稿一覧</h1>

<% @lists.each do |list| %>
<!--each文は、配列やハッシュがもつ要素を順番に取得して処理を実行したいときに使う-->
<!--each文に %=の=はいらない。つけるのは、実際にHTMLとして表示したいものに。-->
  <p>タイトル</p>

  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>

  <p>画像</p>
  <% if list.image.attached? %>
    <%= image_tag list.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>
  
<% end %>